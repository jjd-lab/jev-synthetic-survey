# Runtime dependencies. Development tools are in requirements-dev.txt.
#
# Scoring the shipped runs needs only numpy, scipy and pandas. The rest is needed to run a
# new arm.

# LangChain ecosystem (1.x LTS), pinned exactly: the gpt-4.1 arms were run against these
# versions and structured-output behavior has moved between minor releases before.
langchain==1.3.7
langchain-core==1.4.6
langchain-openai==1.3.0

pydantic>=2.0.0
python-dotenv>=1.0.0
scipy>=1.11.0
numpy>=1.24.0
pyyaml>=6.0.0
pandas>=2.0.0

openpyxl>=3.1.0  # reads the respondent-level workbooks under runs/

# Public-data fetch (scripts/twin2k/fetch_twin2k.py); `data/` is git-ignored, so a fresh
# clone has to download Twin-2K-500 before any twin2k config will run.
huggingface-hub>=1.28.0
# Declared for `pd.read_parquet` in src/data/preprocessors/twin2k.py (the per-respondent
# pricing prices).
pyarrow>=14.0.0
# Direct HTTP for the TypeSafe/Jev endpoint (scripts/twin2k/jev_client.py), which is reached
# outside the LangChain client path.
requests>=2.31.0
# Imported directly by src/utils/llm_factory.py for its retry wrapper. Arrives transitively
# from langchain-core, so declaring it only stops a future prune from breaking retries.
tenacity>=8.0
